package cpu;

public class DatatypeTest {
/*
    @Test
    public void increment_0x0000Plus5_Returns0x0005() {
        // arrange
        DWord expected = Datatype.parseDWord(0x0005);

        // act
        DWord DWord = Datatype.parseDWord(0x0000);
        Datatype.increment(DWord, 5);

        // assert
        assertEquals(expected, DWord);
    }

    @Test
    public void increment_0xFFFFPlus1_Returns0x00000() {
        // arrange
        DWord expected = Datatype.parseDWord(0x0000);

        // act
        DWord DWord = Datatype.parseDWord(0xFFFF);
        Datatype.increment(DWord, 1);

        // assert
        assertEquals(expected, DWord);
    }

    @Test
    public void decrement_0x0005Subs1_Returns0x0000() {
        // arrange
        DWord expected = Datatype.parseDWord(0x0000);

        // act
        DWord DWord = Datatype.parseDWord(0x0005);
        Datatype.decrement(DWord, 5);

        // assert
        assertEquals(expected, DWord);
    }

    @Test
    public void decrement_0x0000Subs1_Returns0xFFFF() {
        // arrange
        DWord expected = Datatype.parseDWord(0xFFFF);

        // act
        DWord DWord = Datatype.parseDWord(0x0000);
        Datatype.decrement(DWord, 1);

        // assert
        assertEquals(expected, DWord);
    }*/
}
