package memory;

import core.MemoryAddr;

public class MemoryMapTest {
/*
    MemoryMap memoryMap = MemoryMap.getInstance();

    @Before
    public void setUp() {
        memoryMap.clear();
    }

    @Test
    public void loadByte_FirstPosition() {
        // arrange
        MemoryAddr address = MemoryAddr.getByAddress(0x0000);
        byte data = (byte) 0xABCD;

        // act
        memoryMap.loadByte(address, data);

        // assert
        assertEquals(memoryMap.retrieveByte(address), data);
    }

    @Test
    public void loadByte_LastPosition() {
        // arrange
        MemoryAddr address = MemoryAddr.getByAddress(0xFFFE);
        byte data = (byte) 0xABCD;

        // act
        memoryMap.loadByte(address, data);

        // assert
        assertEquals(memoryMap.retrieveByte(address), data);
    }*/
}
