package memory;

public class MemoryMapTest {
/*
    Memory memory = Memory.getInstance();

    @Before
    public void setUp() {
        memory.clear();
    }

    @Test
    public void loadByte_FirstPosition() {
        // arrange
        MemoryAddr address = MemoryAddr.getByAddress(0x0000);
        byte common = (byte) 0xABCD;

        // act
        memory.loadByte(address, common);

        // assert
        assertEquals(memory.retrieveByte(address), common);
    }

    @Test
    public void loadByte_LastPosition() {
        // arrange
        MemoryAddr address = MemoryAddr.getByAddress(0xFFFE);
        byte common = (byte) 0xABCD;

        // act
        memory.loadByte(address, common);

        // assert
        assertEquals(memory.retrieveByte(address), common);
    }*/
}
